
let myVariable = 5;

if(!(myVariable === 5)) {
    console.log(true);
    console.log(!(myVariable === 5));
} else {
    console.log(false);
    console.log(!(myVariable === 5));
}



// Notes : 

// Variable Scoping :
// The return statement tells the browser to return the result variable out of the function so it is available to use. 
// This is necessary because variables defined inside functions are only available inside those functions. 
// This is called variable scoping.

// Anonymous Function : 
// The function we just passed to addEventListener() here is called an anonymous function, 

// because it doesn't have a name. 