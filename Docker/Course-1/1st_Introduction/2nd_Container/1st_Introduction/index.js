
/*

1. We can create container at top of the Virtaul Machine.
2. We can create container at top of the Physical Server.
*/

// Container :
/* 
Container is a light weight package because container do not have complete OS(Operating System). Instead Container uses the resource from the base OS or the OS on which they are running on.
Package(Base Image) is a combination of Application + Libraries(Dependecies) + System Dependencies(Node.JS, JAVA, PAYTHON, PHP)
*/