
// When the collection is small
// When the collection is frequently updated means write operation is happening more in the collection.
// When the queries are complex means if queries are coming with multiple fields.
// When the collection is large means if collection have many fields then we should not create indexes for many or all property fields.
    // We should create indexes for few property fields of collection.